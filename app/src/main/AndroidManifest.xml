<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.checklist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.TempEstresseActivity"></activity>
        <activity android:name=".activity.TempAmbienteActivity" />
        <activity android:name=".activity.CoolerActivity" />
        <activity android:name=".activity.AudioActivity" />
        <activity android:name=".activity.SistemaActivity" />
        <activity android:name=".activity.JackActivity" />
        <activity android:name=".activity.WebCamActivity" />
        <activity android:name=".activity.SaidaP2Activity" />
        <activity android:name=".activity.TouchPadActivity" />
        <activity android:name=".activity.TecladoActivity" />
        <activity android:name=".activity.RamActivity" />
        <activity android:name=".activity.RedeActivity" />
        <activity android:name=".activity.WifiActivity" />
        <activity android:name=".activity.TelaActivity" />
        <activity android:name=".activity.HDActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>